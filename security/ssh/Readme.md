Get/Create SSH Keypair manually using the below script file.
```sh
$ ./public_key-create.sh
```

1. EC2 Server:  created having <code>public key (.pub)</code>.
2. User:        uses <code>private key (.pem)</code> to connect to EC2 Server